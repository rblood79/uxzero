


const App = (props) => {
    return (
      <footer className={props.position === 'fix' ? "foot active" : "foot"}>
         ministry of national defense.
      </footer>
    );
  }
  
  //App.defaultProps = {};
  
  export default App;